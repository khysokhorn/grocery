import 'package:flutter/material.dart';

class ExploreView1 extends StatelessWidget {
  const ExploreView1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
